// --------------------------------------------------------------------
//	圧縮機
// ====================================================================
//	8th/Dec.2021  t.hara
// --------------------------------------------------------------------

#ifndef __CCOMPRESSOR_H__
#define __CCOMPRESSOR_H__

#include <vector>
#include "cbitmap.hpp"

// --------------------------------------------------------------------
class ccompressor {
private:
	std::vector<unsigned char> image;
	std::vector<unsigned char> compressed;

	// --------------------------------------------------------------------
	unsigned char get( int index );

public:
	// --------------------------------------------------------------------
	//	コンストラクタ
	// --------------------------------------------------------------------
	ccompressor(){
	}

	// --------------------------------------------------------------------
	//	コンバータ
	// --------------------------------------------------------------------
	void converter( const cbitmap &bmp );

	// --------------------------------------------------------------------
	//	圧縮
	// --------------------------------------------------------------------
	void run( void );

	// --------------------------------------------------------------------
	//	保存
	// --------------------------------------------------------------------
	void save( const char *p_file_name );

	// --------------------------------------------------------------------
	//	伸張
	// --------------------------------------------------------------------
	void decompress( cbitmap &decode );
};

#endif
